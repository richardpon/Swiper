#include "mymath.h"

int abs(int input){
    if (input > 0)
        return input;
    else
        return -input;
}

int max(int a, int b){
    if(a>b)
        return a;
    else
        return b;
}

